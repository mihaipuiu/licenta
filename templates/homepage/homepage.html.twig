{% extends 'base/base.html.twig' %}
{% block content %}

    <h2>Our Most Viewed Hotels</h2>
    <div class="row image-box style3">

        {% for hotel in mostViewed %}

            <div class="col-sms-6 col-sm-6 col-md-3">
                <article class="box">
                    <figure class="animated fadeInDown" data-animation-type="fadeInDown" data-animation-delay="0" style="animation-duration: 1s; visibility: visible;">
                        <a title="" href="{{ path('slideshow_popup', {'id': hotel.id}) }}" class="hover-effect popup-gallery"><img width="270" height="160" alt="" src="{{ asset(hotel.hotelPhotos.first.photoFilename) }}"></a>
                    </figure>
                    <div class="details text-center">
                        <h4 class="box-title"><a href="{{ path('hotel_detailed', {'id':hotel.id}) }}">{{ hotel.name }}<small><i class="soap-icon-departure yellow-color"></i> {{ hotel.city.name }}, {{ hotel.city.country.name }}</small></a></h4>
                        <div class="amenities" style="padding-top: 5px; padding-bottom: 5px">
                            {% if hotel.hotelFacility.hasWifi %}<i class="soap-icon-wifi circle" title="WiFi"></i>{% endif %}
                            {% if hotel.hotelFacility.hasFitness %}<i class="soap-icon-fitnessfacility circle" title="FITNESS"></i>{% endif %}
                            {% if hotel.hotelFacility.hasRestaurant %}<i class="soap-icon-fork circle" title="RESTAURANT"></i>{% endif %}
                            {% if hotel.hotelFacility.hasTv %}<i class="soap-icon-television circle" title="TV"></i>{% endif %}
                            {% if hotel.hotelFacility.hasPool %}<i class="soap-icon-swimming circle" title="SWIMMING POOL"></i></i>{% endif %}
                        </div>
                        <div class="five-stars-container">
                            <span class="five-stars" style="width: {{ hotel.overallRating * 20 }}%;"></span>
                        </div>
                        <p class="description">{{ hotel.shortDescription }}</p>
                        <a class="button" href="{{ path('hotel_detailed', {'id':hotel.id}) }}">SEE DETAILS</a>
                    </div>
                </article>
            </div>

        {% endfor %}

    </div>

    <h2>Highest Rated Hotels</h2>
    <div class="row image-box style3">

        {% for hotel in highestRated %}

            <div class="col-sms-6 col-sm-6 col-md-3">
                <article class="box">
                    <figure class="animated fadeInDown" data-animation-type="fadeInDown" data-animation-delay="0" style="animation-duration: 1s; visibility: visible;">
                        <a title="" href="{{ path('slideshow_popup', {'id': hotel.id}) }}" class="hover-effect popup-gallery"><img width="270" height="160" alt="" src="{{ asset(hotel.hotelPhotos.first.photoFilename) }}"></a>
                    </figure>
                    <div class="details text-center">
                        <h4 class="box-title"><a href="{{ path('hotel_detailed', {'id':hotel.id}) }}">{{ hotel.name }}<small><i class="soap-icon-departure yellow-color"></i> {{ hotel.city.name }}, {{ hotel.city.country.name }}</small></a></h4>
                        <div class="amenities" style="padding-top: 5px; padding-bottom: 5px">
                            {% if hotel.hotelFacility.hasWifi %}<i class="soap-icon-wifi circle" title="WiFi"></i>{% endif %}
                            {% if hotel.hotelFacility.hasFitness %}<i class="soap-icon-fitnessfacility circle" title="FITNESS"></i>{% endif %}
                            {% if hotel.hotelFacility.hasRestaurant %}<i class="soap-icon-fork circle" title="RESTAURANT"></i>{% endif %}
                            {% if hotel.hotelFacility.hasTv %}<i class="soap-icon-television circle" title="TV"></i>{% endif %}
                            {% if hotel.hotelFacility.hasPool %}<i class="soap-icon-swimming circle" title="SWIMMING POOL"></i></i>{% endif %}
                        </div>
                        <div class="five-stars-container">
                            <span class="five-stars" style="width: {{ hotel.overallRating * 20 }}%;"></span>
                        </div>
                        <p class="description">{{ hotel.shortDescription }}</p>
                        <a class="button" href="{{ path('hotel_detailed', {'id':hotel.id}) }}">SEE DETAILS</a>
                    </div>
                </article>
            </div>

        {% endfor %}

    </div>

{% endblock %}